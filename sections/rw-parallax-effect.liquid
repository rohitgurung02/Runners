{%- assign sec_id = '#shopify-section-' | append: section.id -%}
{%- assign parts = sec_id | split: '_' -%}
{%- assign last_lower = parts | last -%}
{%- assign last_part = last_lower | downcase -%}

<pc-parallax-{{ last_part }} class="pc-parallax-container {% unless section.settings.full_width %}pc-page-width{% endunless %}">
  {%- for block in section.blocks -%}
    {%- if block.settings.image != blank -%}
      <div class="pc-parallax pc-parallax-{{ forloop.index }} {% if section.settings.enable_overlay %}pc-parallax-overlay-{{ last_part }}{% endif %}"
        style="
          background-image: url('{{ block.settings.image | image_url: width: 2000 }}');
          height: {{ section.settings.height }}px;
          background-position: center top;
        ">
        <div class="pc-text-block-{{ last_part }}" style="text-align: {{ section.settings.text_alignment }};">
          {%- if block.settings.heading != blank -%}
            <div class="pc-heading-{{ last_part }}">
              {{ block.settings.heading }}
            </div>
          {%- endif -%}
          
          {%- if block.settings.subheading != blank -%}
            <div class="pc-subheading-{{ last_part }}">
              {{ block.settings.subheading }}
            </div>
          {%- endif -%}
          
          {%- if block.settings.show_button -%}
            <a href="{{ block.settings.button_link }}" class="pc-button-{{ last_part }} pc-btn pc-btn--tertiary pc-btn--small">
              {{ block.settings.button_label | escape }}
            </a>
          {%- endif -%}
        </div>
      </div>
    {%- else -%}
      <span class="pc-placeholder">
        {{ 'product-1' | placeholder_svg_tag }}
      </span>
    {%- endif -%}
  {%- endfor -%}
</pc-parallax-{{ last_part }}>

<style>
  {{ sec_id }} .pc-parallax-container {
    overflow: hidden;
  }
  
  {{ sec_id }} .pc-parallax {
    position: relative;
    background-repeat: no-repeat;
    background-attachment: fixed;
    background-size: cover;
    width: 100%;
    overflow: hidden;
  }
  
  {{ sec_id }} .pc-text-block-{{ last_part }} {
    position: absolute;
    top: {{ section.settings.text_position }}%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 90%;
  }
  
  {{ sec_id }} .pc-heading-{{ last_part }} {
    font-size: {{ section.settings.heading_size }}px;
    letter-spacing: {{ section.settings.letter_spacing }}px;
    font-weight: bold;
    color: #fff;
    text-shadow: 2px 2px 10px rgba(255, 255, 255, 0.3);
    margin-bottom: {{ section.settings.heading_margin }}px;
  }
  
  {{ sec_id }} .pc-subheading-{{ last_part }} {
    font-size: {{ section.settings.subheading_size }}px;
    letter-spacing: {{ section.settings.letter_spacing }}px;
    color: #fff;
  }
  
  {{ sec_id }} .pc-button-{{ last_part }} {
    font-size: {{ section.settings.button_size }}px;
    letter-spacing: {{ section.settings.letter_spacing }}px;
    border-color: #fff;
    background: transparent;
    color: #fff;
  }
  
  {{ sec_id }} .pc-parallax-overlay-{{ last_part }}::before {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    background-color: {{ section.settings.overlay_color }};
    opacity: {{ section.settings.overlay_opacity }}%;
  }
  
  {{ sec_id }} .pc-page-width {
    max-width: 1200px;
    margin: 0 auto;
  }
  
  @media only screen and (max-width: 767px) {
    {{ sec_id }} .pc-parallax {
      background-attachment: scroll;
    }
    
    {{ sec_id }} .pc-heading-{{ last_part }} {
      font-size: {{ section.settings.mobile_heading_size }}px;
    }
    
    {{ sec_id }} .pc-subheading-{{ last_part }} {
      font-size: {{ section.settings.mobile_subheading_size }}px;
    }
  }
</style>

{% schema %}
{
  "name": "PWC - Parallax #1",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "checkbox",
      "id": "full_width",
      "label": "Full width",
      "default": true
    },
    {
      "type": "range",
      "id": "height",
      "label": "Section height",
      "min": 200,
      "max": 1000,
      "step": 10,
      "unit": "px",
      "default": 400
    },
    {
      "type": "header",
      "content": "Text Settings"
    },
    {
      "type": "select",
      "id": "text_alignment",
      "label": "Text alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "left"
    },
    {
      "type": "range",
      "id": "text_position",
      "label": "Text vertical position",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "%",
      "default": 50
    },
    {
      "type": "range",
      "id": "heading_size",
      "label": "Heading size",
      "min": 12,
      "max": 72,
      "step": 1,
      "unit": "px",
      "default": 55
    },
    {
      "type": "range",
      "id": "subheading_size",
      "label": "Subheading size",
      "min": 12,
      "max": 36,
      "step": 1,
      "unit": "px",
      "default": 25
    },
    {
      "type": "range",
      "id": "button_size",
      "label": "Button text size",
      "min": 12,
      "max": 36,
      "step": 1,
      "unit": "px",
      "default": 25
    },
    {
      "type": "range",
      "id": "letter_spacing",
      "label": "Letter spacing",
      "min": 0,
      "max": 10,
      "step": 0.5,
      "unit": "px",
      "default": 1
    },
    {
      "type": "range",
      "id": "heading_margin",
      "label": "Heading margin bottom",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 10
    },
    {
      "type": "header",
      "content": "Overlay Settings"
    },
    {
      "type": "checkbox",
      "id": "enable_overlay",
      "label": "Enable overlay",
      "default": true
    },
    {
      "type": "color",
      "id": "overlay_color",
      "label": "Overlay color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "label": "Overlay opacity",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "%",
      "default": 50
    },
    {
      "type": "header",
      "content": "Mobile Settings"
    },
    {
      "type": "range",
      "id": "mobile_heading_size",
      "label": "Mobile heading size",
      "min": 12,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 30
    },
    {
      "type": "range",
      "id": "mobile_subheading_size",
      "label": "Mobile subheading size",
      "min": 12,
      "max": 30,
      "step": 1,
      "unit": "px",
      "default": 18
    }
  ],
  "blocks": [
    {
      "type": "image",
      "name": "Parallax Image",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Parallax Heading"
        },
        {
          "type": "text",
          "id": "subheading",
          "label": "Subheading",
          "default": "Parallax subheading text"
        },
        {
          "type": "checkbox",
          "id": "show_button",
          "label": "Show button",
          "default": true
        },
        {
          "type": "text",
          "id": "button_label",
          "label": "Button label",
          "default": "Shop Now"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Button link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "PWC - Parallax #1",
      "category": "Image",
      "blocks": [
        {
          "type": "image"
        }
      ]
    }
  ]
}
{% endschema %}