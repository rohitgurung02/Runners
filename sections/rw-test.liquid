{% assign sec_id = '#shopify-section-' | append: section.id %}
{% assign parts = sec_id | split: '_' %}
{% assign last_lower = parts | last %}
{% assign last_part = last_lower | downcase %}

<pc-product-slider-{{ last_part }} id="pc-product-slider-{{ last_part }}">
  <section class="pc-product__slider-{{ last_part }}">
    <div class="pc-container-{{ last_part }}">
      <div class="pc-section__header-{{ last_part }}">
        <h3 class="pc-heading-{{ last_part }}">{{ section.settings.heading }}</h3>
        <div id="slider-controls-{{ last_part }}" class="pc-slider__controls-{{ last_part }}">
          <button class="pc-prev-btn-{{ last_part }}" aria-label="Previous">
            <svg viewBox="0 0 24 24" width="20" height="20"><path d="M15 6l-6 6 6 6" stroke="currentColor" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </button>
          <button class="pc-next-btn-{{ last_part }}" aria-label="Next">
            <svg viewBox="0 0 24 24" width="20" height="20"><path d="M9 6l6 6-6 6" stroke="currentColor" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </button>
        </div>
      </div>
      <div class="pc-slider__wrapper-{{ last_part }}">
        <div class="pc-slider-{{ last_part }}">
          {% for block in section.blocks %}
            <div class="pc-slider__item-{{ last_part }}">
              <div class="pc-card-{{ last_part }}">
                <div class="pc-card__image-{{ last_part }}">
                  {% if block.settings.image %}
                    {{- block.settings.image
                      | image_url: width: block.settings.image.width, height: block.settings.image.height
                      | image_tag: loading: 'lazy', preload: true, alt: block.settings.image.alt
                    -}}
                  {% else %}
                    {{ 'product-1' | placeholder_svg_tag }}
                  {% endif %}
                </div>
                <div class="pc-card__content-{{ last_part }}">
                  <h4 class="pc-card__title-{{ last_part }}">{{ block.settings.title }}</h4>
                  <p class="pc-card__desc-{{ last_part }}">{{ block.settings.description }}</p>
                  <a href="{{ block.settings.button_link }}" class="pc-card__btn-{{ last_part }}">{{ block.settings.button_label }}</a>
                </div>
              </div>
            </div>
          {% endfor %}
        </div>
      </div>
    </div>
  </section>
</pc-product-slider-{{ last_part }}>

<style>
  {{ sec_id }} .pc-product__slider-{{ last_part }} {
    background-color: {{ section.settings.bg_color }};
    padding: {{ section.settings.padding_top }}px {{ section.settings.padding_side }}px {{ section.settings.padding_bottom }}px;
  }

  {{ sec_id }} .pc-heading-{{ last_part }} {
    font-size: {{ section.settings.heading_font_size }}px;
    color: {{ section.settings.heading_color }};
    font-weight: 700;
  }

  {{ sec_id }} .pc-section__header-{{ last_part }} {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
  }

  {{ sec_id }} .pc-slider__controls-{{ last_part }} {
    display: flex;
    gap: 8px;
  }

  {{ sec_id }} .pc-prev-btn-{{ last_part }},
  {{ sec_id }} .pc-next-btn-{{ last_part }} {
    border: 1px solid #000;
    background: #fff;
    padding: 5px;
    border-radius: 4px;
    cursor: pointer;
  }

  {{ sec_id }} .pc-slider-{{ last_part }} {
    display: flex;
    overflow-x: auto;
    scroll-behavior: smooth;
    gap: 20px;
  }

  {{ sec_id }} .pc-slider__item-{{ last_part }} {
    flex: 0 0 auto;
    width: calc(50% - 10px);
  }

  {{ sec_id }} .pc-card-{{ last_part }} {
    display: flex;
    background: #fff;
    border-radius: 10px;
    overflow: hidden;
    border: 1px solid #ddd;
    box-shadow: {% if section.settings.shadow %}0 2px 8px rgba(0,0,0,0.1){% else %}none{% endif %};
  }

  {{ sec_id }} .pc-card__image-{{ last_part }} {
    flex: 0 0 45%;
  }

  {{ sec_id }} .pc-card__image-{{ last_part }} img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  {{ sec_id }} .pc-card__content-{{ last_part }} {
    padding: 20px;
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  {{ sec_id }} .pc-card__title-{{ last_part }} {
    font-size: {{ section.settings.title_font_size }}px;
    font-weight: bold;
    margin: 0 0 10px;
  }

  {{ sec_id }} .pc-card__desc-{{ last_part }} {
    font-size: 14px;
    opacity: 0.8;
  }

  {{ sec_id }} .pc-card__btn-{{ last_part }} {
    margin-top: 15px;
    padding: 8px 15px;
    text-decoration: none;
    border: 1px solid #000;
    color: #000;
    font-weight: bold;
    display: inline-block;
    width: fit-content;
  }

  @media screen and (max-width: 749px) {
    {{ sec_id }} .pc-slider__item-{{ last_part }} {
      width: 100%;
    }

    {{ sec_id }} .pc-card-{{ last_part }} {
      flex-direction: column;
    }

    {{ sec_id }} .pc-card__image-{{ last_part }} {
      width: 100%;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const container = document.querySelector('.pc-slider-{{ last_part }}');
    const prev = document.querySelector('.pc-prev-btn-{{ last_part }}');
    const next = document.querySelector('.pc-next-btn-{{ last_part }}');

    const scrollStep = container.offsetWidth;

    next.addEventListener('click', () => {
      container.scrollBy({ left: scrollStep, behavior: 'smooth' });
    });

    prev.addEventListener('click', () => {
      container.scrollBy({ left: -scrollStep, behavior: 'smooth' });
    });
  });
</script>

{% schema %}
{
  "name": "PWC - Product Slider #1",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Section Heading",
      "default": "Organic Product Slider"
    },
    {
      "type": "range",
      "id": "heading_font_size",
      "label": "Heading Font Size",
      "min": 12,
      "max": 48,
      "step": 1,
      "unit": "px",
      "default": 24
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#f5f5f5"
    },
    {
      "type": "checkbox",
      "id": "shadow",
      "label": "Enable Card Shadow",
      "default": true
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top Padding (Desktop)",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom Padding (Desktop)",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_side",
      "label": "Side Padding (Desktop)",
      "min": 0,
      "max": 50,
      "step": 5,
      "unit": "px",
      "default": 20
    },
    {
      "type": "range",
      "id": "mobile_padding_top",
      "label": "Top Padding (Mobile)",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "default": 20
    },
    {
      "type": "range",
      "id": "mobile_padding_bottom",
      "label": "Bottom Padding (Mobile)",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "default": 20
    },
    {
      "type": "range",
      "id": "mobile_padding_side",
      "label": "Side Padding (Mobile)",
      "min": 0,
      "max": 50,
      "step": 5,
      "unit": "px",
      "default": 15
    },
    {
      "type": "range",
      "id": "mobile_heading_font_size",
      "label": "Heading Font Size (Mobile)",
      "min": 12,
      "max": 36,
      "step": 1,
      "unit": "px",
      "default": 20
    },
    {
      "type": "color",
      "id": "button_color",
      "label": "Button Color",
      "default": "#000"
    },
    {
      "type": "range",
      "id": "title_font_size",
      "label": "Card Title Font Size",
      "min": 12,
      "max": 28,
      "step": 1,
      "unit": "px",
      "default": 16
    }
  ],
  "blocks": [
    {
      "type": "product_card",
      "name": "Product Card",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Product Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Product Title",
          "default": "Fresh Organic Product"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Product Description",
          "default": "This is a great organic item."
        },
        {
          "type": "text",
          "id": "button_label",
          "label": "Button Label",
          "default": "Shop Now"
        },
      ]
    }
  ],
  "max_blocks": 10,
  "presets": [
    {
      "name": "PWC - Product Slider #1",
      "category": "Products",
      "blocks": [
        {
          "type": "product_card"
        },
        {
          "type": "product_card"
        }
      ]
    }
  ]
}
{% endschema %}