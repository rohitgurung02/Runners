{%- assign sec_id = '#shopify-section-' | append: section.id -%}
{%- assign parts = sec_id | split: '_' -%}
{%- assign last_lower = parts | last -%}
{%- assign last_part = last_lower | downcase -%}

<style>
  {{ sec_id }} * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  {{ sec_id }} {
    min-height: 100vh;
    background: linear-gradient(135deg, {{ section.settings.gradient_start }} 0%, {{ section.settings.gradient_end }} 100%);
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
    overflow-x: hidden;
    position: relative;
  }

  {{ sec_id }} .pc-page-content-{{ last_part }} {
    position: relative;
    z-index: 1;
    padding: {{ section.settings.desktop_padding }}px;
  }

  {{ sec_id }} .pc-grid-pattern-{{ last_part }} {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: radial-gradient(
        circle at 25% 25%,
        rgba(255, 255, 255, 0.1) 2px,
        transparent 2px
      ),
      radial-gradient(
        circle at 75% 75%,
        rgba(255, 255, 255, 0.05) 1px,
        transparent 1px
      );
    background-size: 80px 80px, 40px 40px;
    animation: pc-gridMove-{{ last_part }} 20s linear infinite;
    z-index: 0;
  }

  {{ sec_id }} .pc-floating-elements-{{ last_part }} {
    position: fixed;
    width: 100%;
    height: 100%;
    z-index: 0;
  }

  {{ sec_id }} .pc-floating-element-{{ last_part }} {
    position: absolute;
    border-radius: 50%;
    animation: pc-float-{{ last_part }} 8s ease-in-out infinite;
  }

  {{ sec_id }} .pc-floating-element-{{ last_part }}:nth-child(1) {
    width: 120px;
    height: 120px;
    top: 10%;
    left: 5%;
    background: rgba(255, 107, 107, 0.15);
    animation-delay: 0s;
  }

  {{ sec_id }} .pc-floating-element-{{ last_part }}:nth-child(2) {
    width: 80px;
    height: 80px;
    top: 70%;
    right: 10%;
    background: rgba(78, 205, 196, 0.15);
    animation-delay: 2s;
  }

  {{ sec_id }} .pc-floating-element-{{ last_part }}:nth-child(3) {
    width: 100px;
    height: 100px;
    bottom: 20%;
    left: 70%;
    background: rgba(69, 183, 209, 0.15);
    animation-delay: 4s;
  }

  {{ sec_id }} .pc-text-content-{{ last_part }} {
    text-align: center;
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: 3rem;
  }

  {{ sec_id }} .pc-main-title-{{ last_part }} {
    font-size: {{ section.settings.title_size }}px;
    font-weight: 700;
    margin-bottom: 0.5rem;
    background: linear-gradient(45deg, #fff, #e0e0e0);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  {{ sec_id }} .pc-effect-label-{{ last_part }} {
    font-size: {{ section.settings.label_size }}px;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 1rem;
    letter-spacing: 2px;
    text-transform: uppercase;
  }

  {{ sec_id }} .pc-subtitle-{{ last_part }} {
    font-size: {{ section.settings.subtitle_size }}px;
    font-weight: 300;
    opacity: 0.8;
    margin-bottom: 2rem;
  }

  {{ sec_id }} .pc-feature-grid-{{ last_part }} {
    display: grid;
    grid-template-columns: repeat({{ section.settings.grid_columns }}, 1fr);
    gap: {{ section.settings.grid_gap }}px;
    max-width: 1000px;
    margin: 0 auto;
  }

  {{ sec_id }} .pc-feature-item-{{ last_part }} {
    background: rgba(255, 255, 255, 0.1);
    padding: 1.5rem;
    border-radius: 16px;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    transition: all 0.3s ease;
  }

  {{ sec_id }} .pc-feature-item-{{ last_part }}:hover {
    background: rgba(255, 255, 255, 0.15);
    transform: translateY(-5px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
  }

  {{ sec_id }} .pc-feature-item-{{ last_part }} h3 {
    font-size: {{ section.settings.feature_title_size }}px;
    margin-bottom: 0.5rem;
    color: white;
  }

  {{ sec_id }} .pc-feature-item-{{ last_part }} p {
    font-size: {{ section.settings.feature_text_size }}px;
    opacity: 0.8;
    line-height: 1.4;
    color: white;
    margin: 0;
  }

  @keyframes pc-gridMove-{{ last_part }} {
    0% {
      transform: translate(0, 0);
    }
    100% {
      transform: translate(80px, 80px);
    }
  }

  @keyframes pc-float-{{ last_part }} {
    0%, 100% {
      transform: translateY(0px) scale(1);
      opacity: 0.7;
    }
    50% {
      transform: translateY(-20px) scale(1.1);
      opacity: 1;
    }
  }

  @media (max-width: 768px) {
    {{ sec_id }} .pc-main-title-{{ last_part }} {
      font-size: {{ section.settings.mobile_title_size }}px;
    }

    {{ sec_id }} .pc-feature-grid-{{ last_part }} {
      grid-template-columns: 1fr;
      gap: {{ section.settings.mobile_grid_gap }}px;
    }

    {{ sec_id }} .pc-page-content-{{ last_part }} {
      padding: {{ section.settings.mobile_padding }}px;
    }
    
    {{ sec_id }} .pc-effect-label-{{ last_part }} {
      font-size: {{ section.settings.mobile_label_size }}px;
    }
  }
</style>

<div class="pc-grid-pattern-{{ last_part }}"></div>
<div class="pc-floating-elements-{{ last_part }}">
  <div class="pc-floating-element-{{ last_part }}"></div>
  <div class="pc-floating-element-{{ last_part }}"></div>
  <div class="pc-floating-element-{{ last_part }}"></div>
</div>

<div class="pc-page-content-{{ last_part }}">

  <div class="pc-feature-grid-{{ last_part }}">
    {% for block in section.blocks %}
      <div class="pc-feature-item-{{ last_part }}" {{ block.shopify_attributes }}>
        <h3>{{ block.settings.feature_title }}</h3>
        <p>{{ block.settings.feature_description }}</p>
      </div>
    {% endfor %}
  </div>
</div>

{% schema %}
{
  "name": "PWC - Feature Grid #1",
  "settings": [
    {
      "type": "header",
      "content": "General Settings"
    },
    {
      "type": "text",
      "id": "effect_label",
      "label": "Effect Label",
      "default": "Feature Grid"
    },
    {
      "type": "text",
      "id": "main_title",
      "label": "Main Title",
      "default": "Interactive Design"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Experience our premium features"
    },
    {
      "type": "color",
      "id": "gradient_start",
      "label": "Gradient Start Color",
      "default": "#667eea"
    },
    {
      "type": "color",
      "id": "gradient_end",
      "label": "Gradient End Color",
      "default": "#764ba2"
    },
    {
      "type": "header",
      "content": "Typography Settings"
    },
    {
      "type": "range",
      "id": "label_size",
      "label": "Label Size (Desktop)",
      "min": 10,
      "max": 24,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "mobile_label_size",
      "label": "Label Size (Mobile)",
      "min": 10,
      "max": 20,
      "step": 1,
      "unit": "px",
      "default": 14
    },
    {
      "type": "range",
      "id": "title_size",
      "label": "Title Size (Desktop)",
      "min": 20,
      "max": 80,
      "step": 1,
      "unit": "px",
      "default": 64
    },
    {
      "type": "range",
      "id": "mobile_title_size",
      "label": "Title Size (Mobile)",
      "min": 16,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 40
    },
    {
      "type": "range",
      "id": "subtitle_size",
      "label": "Subtitle Size",
      "min": 12,
      "max": 30,
      "step": 1,
      "unit": "px",
      "default": 24
    },
    {
      "type": "range",
      "id": "feature_title_size",
      "label": "Feature Title Size",
      "min": 12,
      "max": 30,
      "step": 1,
      "unit": "px",
      "default": 19
    },
    {
      "type": "range",
      "id": "feature_text_size",
      "label": "Feature Text Size",
      "min": 10,
      "max": 20,
      "step": 1,
      "unit": "px",
      "default": 14
    },
    {
      "type": "header",
      "content": "Desktop Settings"
    },
    {
      "type": "range",
      "id": "desktop_padding",
      "label": "Desktop Padding",
      "min": 5,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 32
    },
    {
      "type": "range",
      "id": "grid_columns",
      "label": "Grid Columns",
      "min": 1,
      "max": 4,
      "step": 1,
      "default": 3
    },
    {
      "type": "range",
      "id": "grid_gap",
      "label": "Grid Gap",
      "min": 5,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 20
    },
    {
      "type": "header",
      "content": "Mobile Settings"
    },
    {
      "type": "range",
      "id": "mobile_padding",
      "label": "Mobile Padding",
      "min": 5,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "mobile_grid_gap",
      "label": "Mobile Grid Gap",
      "min": 5,
      "max": 30,
      "step": 1,
      "unit": "px",
      "default": 10
    }
  ],
  "blocks": [
    {
      "type": "feature",
      "name": "Feature Item",
      "settings": [
        {
          "type": "text",
          "id": "feature_title",
          "label": "Feature Title",
          "default": "Feature Title"
        },
        {
          "type": "textarea",
          "id": "feature_description",
          "label": "Feature Description",
          "default": "Feature description text goes here"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "PWC - Feature Grid #1",
      "category": "Content",
      "blocks": [
        {
          "type": "feature",
          "settings": {
            "feature_title": "Precision Design",
            "feature_description": "Carefully crafted components with attention to detail"
          }
        },
        {
          "type": "feature",
          "settings": {
            "feature_title": "Responsive Layout",
            "feature_description": "Adapts perfectly to all screen sizes and devices"
          }
        },
        {
          "type": "feature",
          "settings": {
            "feature_title": "Smooth Animations",
            "feature_description": "Subtle animations that enhance user experience"
          }
        },
        {
          "type": "feature",
          "settings": {
            "feature_title": "Modern Aesthetics",
            "feature_description": "Clean design with glass morphism effects"
          }
        },
        {
          "type": "feature",
          "settings": {
            "feature_title": "Easy Customization",
            "feature_description": "Fully customizable through theme settings"
          }
        },
        {
          "type": "feature",
          "settings": {
            "feature_title": "Performance Optimized",
            "feature_description": "Lightweight code for fast loading times"
          }
        }
      ]
    }
  ]
}
{% endschema %}