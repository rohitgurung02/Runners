{%- assign sec_id = '#shopify-section-' | append: section.id -%}
{%- assign parts = sec_id | split: '_' -%}
{%- assign last_lower = parts | last -%}
{%- assign last_part = last_lower | downcase -%}

<style>
  {{ sec_id }} .pc-filterable-gallery {
    position: relative;
    z-index: 1;
    padding-top: {{ section.settings.desktop_padding_top }}px;
    padding-bottom: {{ section.settings.desktop_padding_bottom }}px;
    background-color: {{ section.settings.background_color }};
  }

  {{ sec_id }} .pc-container {
    max-width: {{ section.settings.max_width }}px;
    margin: 0 auto;
    padding: 0 15px;
  }

  {{ sec_id }} .pc-text-center {
    text-align: center;
    margin-bottom: {{ section.settings.title_bottom_margin }}px;
  }

  {{ sec_id }} .pc-title {
    font-size: {{ section.settings.heading_size }}px;
    font-weight: {{ section.settings.heading_weight }};
    font-family: {{ section.settings.heading_font.family }}, {{ section.settings.heading_font.fallback_families }};
    color: {{ section.settings.heading_color }};
    margin-bottom: {{ section.settings.heading_bottom_margin }}px;
  }

  {{ sec_id }} .pc-subtitle {
    font-size: {{ section.settings.subheading_size }}px;
    color: {{ section.settings.subheading_color }};
    max-width: {{ section.settings.subheading_max_width }}px;
    margin: 0 auto;
  }

  {{ sec_id }} .pc-filter {
    text-align: center;
    max-width: 1050px;
    margin: 0 auto {{ section.settings.filter_bottom_margin }}px;
  }

  {{ sec_id }} .pc-btn {
    padding: {{ section.settings.filter_vertical_padding }}px {{ section.settings.filter_horizontal_padding }}px;
    margin: 5px 4px 4px 0;
    display: inline-block;
    color: {{ section.settings.filter_text_color }};
    text-decoration: {{ section.settings.button_text_decoration }};
    background: {{ section.settings.filter_bg_color }};
    border: 1px solid {{ section.settings.filter_border_color }};
    transition: all 0.4s;
    border-radius: {{ section.settings.filter_border_radius }}px;
    font-size: {{ section.settings.filter_font_size }}px;
    font-weight: {{ section.settings.filter_font_weight }};
  }

  {{ sec_id }} .pc-btn:hover,
  {{ sec_id }} .pc-btn-active {
    background: {{ section.settings.filter_active_bg_color }};
    color: {{ section.settings.filter_active_text_color }};
    transform: translateY(3px);
  }

  {{ sec_id }} .pc-gallery {
    display: flex;
    justify-content: center;
    width: fit-content;
    max-width: 1320px;
    flex-wrap: wrap;
    margin: {{ section.settings.gallery_top_margin }}px auto;
  }

  {{ sec_id }} .pc-gallery a {
    display: flex;
  }

  {{ sec_id }} .pc-gallery-img {
    width: {{ section.settings.image_width }}px;
    height: {{ section.settings.image_height }}px;
    object-fit: cover;
    transition: 0.3s ease-in-out;
    border-radius: {{ section.settings.image_border_radius }}px;
    overflow: hidden;
    margin: {{ section.settings.image_margin }}px;
  }

  {{ sec_id }} .pc-gallery-img:hover {
    transform: scale(1.1);
  }

  {{ sec_id }} .pc-sets .pc-hide,
  {{ sec_id }} .pc-sets .pc-pophide {
    display: none;
    opacity: 0;
  }

  {{ sec_id }} .pc-closeBtn {
    position: absolute;
    font-size: 22px;
    font-weight: 500;
    right: 25px;
    top: 25px;
    color: white;
    transition: 0.5s linear;
    padding: 8px 40px;
    border-radius: 25px;
    background: red;
    outline-offset: -6px;
    outline: 2px solid #fff;
  }

  {{ sec_id }} .pc-closeBtn:hover {
    cursor: pointer;
    background: white;
    color: black;
    outline: 2px solid #000;
  }

  {{ sec_id }} .pc-openDiv {
    width: 100%;
    height: 100vh;
    background: #000000e7;
    position: fixed;
    top: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    left: 0;
    z-index: 9999;
  }

  {{ sec_id }} .pc-imgPreview {
    width: 70%;
    object-fit: scale-down;
    max-height: 40vw;
    height: auto;
  }

  {{ sec_id }} .pc-prevButton,
  {{ sec_id }} .pc-nextButton {
    transition: 1s linear;
    padding: 10px 35px;
    font-size: 18px;
    border: none;
    color: white;
    background: #0005;
    border-radius: 10px;
    border: 1px solid white;
    margin: 10px;
  }

  {{ sec_id }} .pc-prevButton:hover,
  {{ sec_id }} .pc-nextButton:hover {
    background: #fff;
    color: black;
  }

  /* Responsive styles remain the same */
</style>

<pc-filterable-gallery-{{ last_part }} class="pc-filterable-gallery">
  <div class="pc-container">
    <!-- Section Title -->
    <div class="pc-text-center">
      {%- if section.settings.heading != blank -%}
        <h2 class="pc-title">{{ section.settings.heading }}</h2>
      {%- endif -%}
      {%- if section.settings.subheading != blank -%}
        <p class="pc-subtitle">{{ section.settings.subheading }}</p>
      {%- endif -%}
    </div>

    <!-- Filter Buttons -->
    <div id="pc-btncontainer-{{ last_part }}" class="pc-filter">
      <a class="pc-btn pc-btn-active" href="#all">ALL</a>
      {%- for block in section.blocks -%}
        {%- if block.type == 'product_button' -%}
          <a class="pc-btn" href="#{{ block.settings.button_label | handleize }}">{{ block.settings.button_label }}</a>
        {%- endif -%}
      {%- endfor -%}
    </div>

    <!-- Gallery Images Organized Under Buttons -->
    <div class="pc-gallery pc-sets">
      {%- for block in section.blocks -%}
        {%- if block.type == 'product_images' -%}
          {%- assign product_handle = block.settings.button_label | handleize -%}
          {%- for i in (1..5) -%}
            {%- assign image_key = 'image_' | append: i -%}
            {%- if block.settings[image_key] != blank -%}
              <a class="pc-all pc-{{ product_handle }}">
                {{ block.settings[image_key] | image_url: width: 400 | image_tag: 
                  loading: 'lazy', 
                  class: 'pc-gallery-img',
                  alt: block.settings[image_key].alt | escape
                }}
              </a>
            {%- endif -%}
          {%- endfor -%}
        {%- endif -%}
      {%- endfor -%}
    </div>
  </div>
</pc-filterable-gallery-{{ last_part }}>

<script>
  class PCFilterableGallery{{ last_part | capitalize }} extends HTMLElement {
    constructor() {
      super();
      this.initializeGallery();
    }

    initializeGallery() {
      // Filter functionality
      const filterButtons = this.querySelectorAll(`#pc-btncontainer-${'{{ last_part }}'} .pc-btn`);
      filterButtons.forEach(button => {
        button.addEventListener('click', (e) => {
          e.preventDefault();
          const filterValue = button.getAttribute('href').substr(1);
          const galleryItems = this.querySelectorAll('.pc-sets a');
          
          galleryItems.forEach(item => {
            if (filterValue === "all") {
              item.classList.remove('pc-hide');
            } else {
              const hasClass = item.classList.contains(`pc-${filterValue}`);
              item.classList.toggle('pc-hide', !hasClass);
            }
          });

          // Update active button
          filterButtons.forEach(btn => btn.classList.remove('pc-btn-active'));
          button.classList.add('pc-btn-active');
        });
      });

      // Image preview functionality
      const images = this.querySelectorAll('.pc-gallery-img');
      images.forEach((img, index) => {
        img.addEventListener('click', (e) => {
          if (e.target === img) {
            let currentIndex = index;
            const openDiv = document.createElement('div');
            const imgPreview = document.createElement('img');
            const buttonsSection = document.createElement('div');
            buttonsSection.classList.add('pc-buttonsSection');
            
            const closeBtn = document.createElement('button');
            const nextBtn = document.createElement('button');
            const prevButton = document.createElement('button');
            
            prevButton.innerText = 'Previous';
            nextBtn.innerText = 'Next';
            nextBtn.classList.add('pc-nextButton');
            prevButton.classList.add('pc-prevButton');
            
            nextBtn.addEventListener('click', () => {
              currentIndex = (currentIndex + 1) % images.length;
              imgPreview.src = images[currentIndex].src;
            });

            prevButton.addEventListener('click', () => {
              currentIndex = (currentIndex - 1 + images.length) % images.length;
              imgPreview.src = images[currentIndex].src;
            });

            closeBtn.classList.add('pc-closeBtn');
            closeBtn.innerText = 'Close';
            closeBtn.addEventListener('click', () => {
              openDiv.remove();
            });

            imgPreview.classList.add('pc-imgPreview');
            imgPreview.src = img.src;

            buttonsSection.append(prevButton, nextBtn);
            openDiv.append(imgPreview, buttonsSection, closeBtn);
            openDiv.classList.add('pc-openDiv');

            document.body.append(openDiv);
          }
        });
      });
    }
  }

  customElements.define('pc-filterable-gallery-{{ last_part }}', PCFilterableGallery{{ last_part | capitalize }});
</script>

{% schema %}
{
  "name": "Product Filter Gallery",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Layout Settings"
    },
    {
      "type": "range",
      "id": "max_width",
      "label": "Max Width",
      "min": 800,
      "max": 1800,
      "step": 10,
      "unit": "px",
      "default": 1320
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "desktop_padding_top",
      "label": "Top Padding (Desktop)",
      "min": 0,
      "max": 200,
      "step": 5,
      "unit": "px",
      "default": 80
    },
    {
      "type": "range",
      "id": "desktop_padding_bottom",
      "label": "Bottom Padding (Desktop)",
      "min": 0,
      "max": 200,
      "step": 5,
      "unit": "px",
      "default": 80
    },
    {
      "type": "header",
      "content": "Title Settings"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Our Products"
    },
    {
      "type": "range",
      "id": "heading_size",
      "label": "Heading Size",
      "min": 20,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 46
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading Color",
      "default": "#333333"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Browse our collection"
    },
    {
      "type": "range",
      "id": "subheading_size",
      "label": "Subheading Size",
      "min": 12,
      "max": 36,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "color",
      "id": "subheading_color",
      "label": "Subheading Color",
      "default": "#666666"
    },
    {
      "type": "header",
      "content": "Button Settings"
    },
    {
      "type": "color",
      "id": "filter_bg_color",
      "label": "Button Background",
      "default": "#f5f5f5"
    },
    {
      "type": "color",
      "id": "filter_active_bg_color",
      "label": "Active Button Background",
      "default": "#333333"
    },
    {
      "type": "range",
      "id": "filter_border_radius",
      "label": "Button Border Radius",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 25
    },
    {
      "type": "header",
      "content": "Image Settings"
    },
    {
      "type": "range",
      "id": "image_width",
      "label": "Image Width (Desktop)",
      "min": 100,
      "max": 400,
      "step": 10,
      "unit": "px",
      "default": 250
    },
    {
      "type": "range",
      "id": "image_height",
      "label": "Image Height (Desktop)",
      "min": 100,
      "max": 500,
      "step": 10,
      "unit": "px",
      "default": 250
    },
    {
      "type": "range",
      "id": "image_border_radius",
      "label": "Image Border Radius",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 8
    }
  ],
  "blocks": [
    {
      "type": "product_button",
      "name": "Product Button",
      "settings": [
        {
          "type": "text",
          "id": "button_label",
          "label": "Button Label",
          "default": "Product"
        }
      ]
    },
    {
      "type": "product_images",
      "name": "Product Images",
      "settings": [
        {
          "type": "text",
          "id": "button_label",
          "label": "Associated Button Label",
          "info": "Must match exactly with a button label"
        },
        {
          "type": "image_picker",
          "id": "image_1",
          "label": "Image 1"
        },
        {
          "type": "image_picker",
          "id": "image_2",
          "label": "Image 2"
        },
        {
          "type": "image_picker",
          "id": "image_3",
          "label": "Image 3"
        },
        {
          "type": "image_picker",
          "id": "image_4",
          "label": "Image 4"
        },
        {
          "type": "image_picker",
          "id": "image_5",
          "label": "Image 5"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Product Filter Gallery",
      "blocks": [
        {
          "type": "product_button",
          "settings": {
            "button_label": "Shoes"
          }
        },
        {
          "type": "product_images",
          "settings": {
            "button_label": "Shoes",
            "image_1": "shopify:\/\/shop_images\/shoe-1.jpg",
            "image_2": "shopify:\/\/shop_images\/shoe-2.jpg"
          }
        },
        {
          "type": "product_button",
          "settings": {
            "button_label": "Bags"
          }
        },
        {
          "type": "product_images",
          "settings": {
            "button_label": "Bags",
            "image_1": "shopify:\/\/shop_images\/bag-1.jpg",
            "image_2": "shopify:\/\/shop_images\/bag-2.jpg"
          }
        }
      ]
    }
  ]
}
{% endschema %}