
<div class="marquee-horizontal tw-relative tw-flex tw-overflow-hidden tw-w-full tw-justify-start tw-items-center">
  <div class="track-horizontal-alt">
    {% for i in (1..5) %}
      <div class="marquee-element marquee-text">
        <span class="marquee_text tw-font-transducer-extended tw-font-black tw-align-super" style="font-size: 74px;line-height: 74px">
          {{ section.settings.main_text }}
        </span>
      </div>
      
      <div class="marquee-element marquee-text">
        <span class="marquee_text tw-align-super" style="font-size: 74px;line-height: 74px">
          {{ section.settings.emoji }}
        </span>
      </div>
      
      <div class="marquee-element marquee-text-block">
        <div class="marquee_text-block tw-font-transducer" style="font-size: 16px; line-height: 16px;">
          {{ section.settings.secondary_text }}
        </div>
      </div>
      
     
    {% endfor %}
  </div>
</div>

{% javascript %}
document.addEventListener('DOMContentLoaded', function() {
  // JavaScript for marquee functionality if needed
});
{% endjavascript %}

{% style %}
.marquee-horizontal {
  overflow: hidden;
  position: relative;
  width: 100%;
}

.track-horizontal-alt {
  display: flex;
  align-items: center;
  white-space: nowrap;
  will-change: transform;
  animation: scroll-horizontal {{ section.settings.speed }}s linear infinite;
}

.marquee-element {
  margin-right: 40px;
}

@keyframes scroll-horizontal {
  0% { transform: translateX(0); }
  100% { transform: translateX(-50%); }
}
{% endstyle %}


<style>
  .marquee-horizontal {
    overflow: hidden;
    position: relative;
  }
  
  .track-horizontal-alt {
    animation: scroll-horizontal 20s linear infinite;
    display: flex;
    align-items: center;
  }
  
  .marquee-element {
    margin-right: 40px; /* Adjust spacing between elements */
  }
  
  @keyframes scroll-horizontal {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }
</style>

{% schema %}
{
  "name": "Marquee Scroller",
  "settings": [
    {
      "type": "text",
      "id": "main_text",
      "label": "Main Text",
      "default": "TEES"
    },
    {
      "type": "text",
      "id": "emoji",
      "label": "Emoji/Symbol",
      "default": "🚀"
    },
    {
      "type": "richtext",
      "id": "secondary_text",
      "label": "Secondary Text",
      "default": "<p>MAKE YOUR<br>MARK WITH<br>MRBEAST</p>"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "range",
      "id": "speed",
      "min": 5,
      "max": 30,
      "step": 1,
      "unit": "s",
      "label": "Animation Speed",
      "default": 10
    }
  ],
  "presets": [
    {
      "name": "Marquee Scroller",
      "category": "Custom"
    }
  ]
}
{% endschema %}
