{{ 'section-rich-text.css' | asset_url | stylesheet_tag }}

{%- assign sec_id = '#shopify-section-' | append: section.id -%}

 
{%- liquid
  assign speed = section.settings.speed
-%}
 
{%- liquid
  assign speed = section.settings.speed
  assign direction = section.settings.direction
  assign background_color = section.settings.background_color
-%}
 
<style>
  {{ sec_id }} .h2 { font-size: 1.5rem; }
  {{ sec_id }} .h1 { font-size: 2rem; }
  {{ sec_id }} .h0 { font-size: 2.5rem; }
  {{ sec_id }} .hxl { font-size: 3rem; }
  {{ sec_id }} .hxxl { font-size: 3.5rem; }
  {{ sec_id }} .pc-track-horizontal-alt {
    height: 72px;
    display: flex;
    align-items: center;
    gap: 40px;
    width: max-content;
    will-change: transform;
  }
  
  {{ sec_id }} .pc-marquee-element {
    flex-shrink: 0;
    margin-top: 10px;
    display: flex;
    align-items: center;
  }
  
  {{ sec_id }} .pc-marquee_text {
    font-size: 42px;
    line-height: 50px;
    font-weight: 600;
  }
  
  {{ sec_id }} .pc-marquee-image {
    height: 50px;
    width: auto;
    object-fit: contain;
  }

  /* Animation Definitions */
  @keyframes scroll-left {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }

  @keyframes scroll-right {
    0% {
      transform: translateX(-50%);
    }
    100% {
      transform: translateX(0);
    }
  }
  

  /* Apply animation based on direction */
  {{ sec_id }} .pc-track-horizontal-alt {
    animation: {% if direction == 'left' %} scroll-left {% else %} scroll-right {% endif %} {{ speed }}s linear infinite;
  }

  /* Dynamic Background Color */
  {{ sec_id }} .pc-marquee-horizontal {
    background-color: {{ background_color }};
    overflow: hidden;
  }

</style>
 
<div 
  class="pc-marquee-horizontal tw-relative tw-flex tw-overflow-hidden tw-w-full tw-justify-start tw-items-center" 
  style="padding-top: {{ section.settings.padding_top_top }}px; padding-bottom: {{ section.settings.padding_bottom }}px;
  margin-top: {{ section.settings.margin_top }}px; margin-bottom: {{ section.settings.margin_bottom }}px;
 
  background-color: {{ section.settings.background_color }}">
  <div 
    class="pc-track-horizontal-alt" 
    style="animation-duration: {{ section.settings.speed }}s; animation-direction: {{ section.settings.direction }}">
    
    {% for i in (1..2) %}  <!-- Doubled for smooth looping -->
      {% for block in section.blocks %}
        <div class="pc-marquee-element" {{ block.shopify_attributes }}>
          {% case block.type %}
            {% when 'text' %}
             <span class="pc-marquee_text tw-font-transducer-extended tw-font-black tw-align-super {{ block.settings.heading_size }} {% if block.settings.uppercase %}uppercase{% endif %}"
      style="color: {{ block.settings.heading_color }}">
    {{ block.settings.heading }}
</span>
            {% when 'emoji' %}
              <span 
  class="pc-marquee_text tw-font-transducer-extended tw-font-black tw-align-super {{ block.settings.heading_size }}" 
  style="font-size: {{ block.settings.emoji_size }}px;">
  {{ block.settings.emoji }}
</span>
            {% when 'image' %}
              {% if block.settings.image != blank %}
                <img class="pc-marquee-image" src="{{ block.settings.image | img_url: '100x' }}" alt="{{ block.settings.image.alt }}" width="auto" height="50" loading="lazy">
              {% endif %}
          {% endcase %}
        </div>
      {% endfor %}
    {% endfor %}
  </div>
</div>



{% schema %}
{
  "name": "Marquee Scroller",
  "settings": [
    {
      "type": "range",
      "id": "speed",
      "min": 5,
      "max": 30,
      "step": 1,
      "unit": "s",
      "label": "Animation Speed",
      "default": 12
    },
    {
  "type": "select",
  "id": "direction",
  "label": "Animation Direction",
  "options": [
    {
      "value": "left",
      "label": "Left"
    },
    {
      "value": "right",
      "label": "Right"
    }
  ],
  "default": "left"
},
  {
  "type": "select",
  "id": "background_color",
  "label": "Background Color",
  "options": [
    {
      "value": "black",
      "label": "Black"
    },
    {
      "value": "white",
      "label": "White"
    },
    {
      "value": "blue",
      "label": "Blue"
    },
    {
      "value": "orange",
      "label": "Orange"
    },
    {
      "value": "pink",
      "label": "Pink"
    },
    {
      "value": "green",
      "label": "Green"
    },
    {
      "value": "skyblue",
      "label": "Sky Blue"
    },
    {
      "value": "yellow",
      "label": "Yellow"
    },
    {
      "value": "red",
      "label": "Red"
    }
  ],
  "default": "black"
},
     {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 8
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 20
    },
    {
      "type": "range",
      "id": "margin_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Margin Top",
      "default": 12
    },
    {
      "type": "range",
      "id": "margin_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Margin Bottom",
      "default": 20
    },
  ],
  "blocks": [
    {
      "type": "text",
      "name": "Text Item",
      "settings": [
        {
          "type": "inline_richtext",
          "id": "heading",
          "default": "t:sections.rich-text.blocks.heading.settings.heading.default",
          "label": "t:sections.rich-text.blocks.heading.settings.heading.label"
        },
       {
      "type": "select",
      "id": "heading_size",
      "label": "Heading Size",
      "options": [
        { "value": "text-4xl", "label": "Extra Large" },
        { "value": "text-3xl", "label": "Very Large" },
        { "value": "text-2xl", "label": "Large" },
        { "value": "text-xl", "label": "Medium" },
        { "value": "text-lg", "label": "Small" },
        { "value": "text-base", "label": "Normal" },
        { "value": "text-sm", "label": "Very Small" }
      ],
      "default": "text-xl"
    },
        {
          "type": "select",
          "id": "heading_color",
          "label": "Heading Color",
          "options": [
            {
              "value": "black",
              "label": "Black"
            },
            {
              "value": "red",
              "label": "Red"
            },
            {
              "value": "blue",
              "label": "Blue"
            },
            {
              "value": "white",
              "label": "White"
            },
            {
              "value": "green",
              "label": "Green"
            },
            {
              "value": "orange",
              "label": "Orange"
            },
            {
              "value": "purple",
              "label": "Purple"
            }
          ],
          "default": "black"
        }
      ]
    },
    {
      "type": "emoji",
      "name": "Emoji/Symbol",
      "settings": [
        {
          "type": "text",
          "id": "emoji",
          "label": "Emoji or Symbol",
          "default": "🚀"
        },
         {
      "type": "range",
      "id": "emoji_size",
      "min": 12,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Emoji Size",
      "default": 24
    }
      ]
    },
    {
      "type": "image",
      "name": "Image",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "PWC - Scrolling-List #1",
      "blocks": [
        {
          "type": "text"
        },
        {
          "type": "emoji"
        },
        {
          "type": "text"
        }
      ]
    }
  ]
}
{% endschema %}

